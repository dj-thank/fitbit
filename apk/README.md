# APK artifacts

Debug APKs are generated by the public `Android CI` GitHub Actions workflow rather than committed as large binary blobs in Git history.

For v0.2.0, compare the downloaded workflow artifact against `SHA256SUMS.txt` when you need to confirm it matches the verified reference build.
